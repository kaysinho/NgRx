import { IncomeExpenseOrderPipe } from './income-expense-order.pipe';

describe('IncomeExpenseOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new IncomeExpenseOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
